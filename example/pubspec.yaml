name: realtime_audio_example
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  realtime_audio:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  # On-device AEC loopback acceptance gate (integration_test/aec_loopback_test.dart).
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - "assets/audio/"
